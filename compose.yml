version: '3'
services:
  db:
    image: postgres:14
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - '5432:5432'

  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    volumes:
      - .:/rails
      - bundle_data:/usr/local/bundle
    ports:
      - '3000:3000'
    environment:
      RAILS_ENV: development
      EDITOR: vim
      GOOGLE_CLIENT_ID: 1043537618875-nn6cvltkr8s8eua1cillt60800nm0st9.apps.googleusercontent.com
      GOOGLE_CLIENT_SECRET: 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
    depends_on:
      - db

volumes:
  postgres_data:
  bundle_data:
