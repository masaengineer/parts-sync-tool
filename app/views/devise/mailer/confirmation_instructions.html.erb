<%# メールアドレス確認メール %>
<p><%= t('.greeting', email: @email) %></p>

<p><%= t('.message') %></p>

<p><%= link_to t('.action'), confirmation_url(@resource, confirmation_token: @token), class: 'button' %></p>
