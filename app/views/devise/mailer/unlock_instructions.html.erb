<%# アカウントロック解除メール %>
<p><%= t('.greeting', email: @resource.email) %></p>

<p><%= t('.message') %></p>

<p><%= link_to t('.action'), unlock_url(@resource, unlock_token: @token), class: 'button' %></p>
