<!DOCTYPE html>
<html lang="<%= I18n.locale %>" data-theme="light" data-controller="theme">
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "認証" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# SVGファビコン - モダンブラウザ向け %>
    <%= favicon_link_tag 'logo/favicon.svg', type: 'image/svg+xml' %>
    <%# PNGファビコン - レガシーブラウザのフォールバック用 %>
    <%= favicon_link_tag 'logo/favicon.png', type: 'image/png' %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <%# フラッシュメッセージ %>
    <% if notice %>
      <div class="alert alert-info shadow-lg fixed top-4 left-1/2 -translate-x-1/2 z-50 w-auto min-w-[320px] max-w-[90%]">
        <div>
          <iconify-icon icon="lucide:info" class="text-info"></iconify-icon>
          <span><%= notice %></span>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="alert alert-error shadow-lg fixed top-4 left-1/2 -translate-x-1/2 z-50 w-auto min-w-[320px] max-w-[90%]">
        <div>
          <iconify-icon icon="lucide:alert-triangle" class="text-error"></iconify-icon>
          <span><%= alert %></span>
        </div>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
